# Variables d'environnement pour la production (Render)
# ATTENTION: Ne jamais commiter ce fichier avec les vraies valeurs !

NODE_ENV=production
PORT=5000

# Base de données PostgreSQL (Render)
# Remplacer par vos vraies valeurs de connexion
DATABASE_URL=postgresql://username:password@dpg-d1ptb7vfte5s73cnq080-a.oregon-postgres.render.com:5432/database_name

# JWT Secret (à remplacer par une valeur sécurisée)
JWT_SECRET=gospel-super-secret-jwt-key-2025-production

# CORS (URL de votre frontend déployé)
CORS_ORIGIN=*

# Rate limiting (optionnel)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
